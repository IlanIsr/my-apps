# Clerk — all three apps share ONE Clerk instance (same keys) so sessions
# carry across apps. Copy this file to .env.local and fill in the values from
# the Clerk dashboard (API keys). Google + Apple are enabled in the dashboard,
# not here.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
